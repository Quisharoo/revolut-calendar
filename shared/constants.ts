export const APP_TIMEZONE = "Europe/Dublin";

export const MAX_CSV_ROWS = 50_000;

export const MAX_CSV_FILE_BYTES = 5 * 1024 * 1024;

export const CSV_ALLOWED_MIME_TYPES = [
  "text/csv",
  "application/vnd.ms-excel",
  "application/csv",
  "text/plain",
];

export const DEFAULT_CURRENCY_SYMBOL = "$";

export const ICS_MAX_LINE_LENGTH = 75;

export const ICS_CALENDAR_PROD_ID = "-//TransactionCalendar//Recurring Export//EN";
